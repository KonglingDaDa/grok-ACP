# 任务 T2.1：修正 format.test.ts 的无效月份用例

**专题：** 2026-07-04-0340-AI友好化-测试与文档  
**父任务：** T2（PARTIAL）  
**创建时间：** 2026-07-04 04:05

---

## 背景

Task 2 完成后，1 个测试失败：`formatCardTime('2026-13-01T00:00:00Z')` 期望返回 `'—'`，但实际返回 `'13-01 00:00'`。

**PM 决策：** `formatCardTime` 设计为"快速截取显示"，不校验日期合法性（输入已由后端保证合法）。测试用例需要调整，删除"无效月份"断言。

---

## 只允许修改这个文件

1. **修改** `ui/src/lib/format.test.ts` — 调整 `formatCardTime` 的"handles null or invalid input"测试

**禁止**改动其他任何文件。不要改 `format.ts` 的实现。

---

## 修改要求

找到 `ui/src/lib/format.test.ts` 的这一行：

```typescript
expect(formatCardTime('2026-13-01T00:00:00Z')).toBe('—')  // 无效月份
```

**删除这一行**（因为当前实现不校验月份合法性，这个断言与设计不符）。

其余测试保持不变。

---

## 验收（你必须自己执行并在回执中给出结果）

```bash
cd /home/desk/dev/repos/grokACP/ui
npx vitest run src/lib/format.test.ts
```

**预期输出：** 所有测试通过（✓），测试数量从 9 个减少到 8 个。

---

## 收工前自审（小改动，跳过 Critic）

只删除 1 行测试，无需 Critic 自审。

---

## 回复格式

删除的行号、验收输出（必须全绿）。
