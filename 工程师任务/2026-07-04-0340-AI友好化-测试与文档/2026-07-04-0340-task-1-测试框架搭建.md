# 任务 T1：搭建测试框架

**专题：** 2026-07-04-0340-AI友好化-测试与文档  
**PRD：** `./PRD.md`  
**任务编号：** T1  
**创建时间：** 2026-07-04 03:45

---

## 背景

grokACP 当前只有端到端 smoke 测试（`npm run smoke`），缺少单元测试框架。为了后续任务（T2-T5）能够以 TDD 模式开发，需要先搭建测试基础设施。

**PRD 路径：** `工程师任务/2026-07-04-0340-AI友好化-测试与文档/PRD.md`

**关键决策（来自 PRD §2.1）：**
- **测试框架：** Node.js 原生 `node:test`（Node 20+），零 npm 依赖
- **测试文件命名：** `<name>.test.mjs`
- **测试目录：** `test/`

---

## 只允许修改这些文件/目录

1. **新建** `test/` 目录
2. **新建** `test/.gitkeep`（空文件，让 Git 跟踪空目录）
3. **修改** `package.json` — 新增 `"test"` 和 `"test:watch"` 脚本
4. **新建** `test/smoke.test.mjs` — 将现有 smoke 测试改造为测试文件
5. **修改** `CLAUDE.md` — 补充测试章节
6. **修改** `README.md` — 补充测试章节（如果文件存在）

**禁止**改动其他任何文件。禁止新增 npm 依赖（`dependencies` 或 `devDependencies` 必须保持空或不存在）。不要改 `src/` 目录的任何文件（本任务只搭框架，不写实际测试）。

---

## 修改要求

### 修改一：创建 `test/` 目录结构

```bash
mkdir -p test
touch test/.gitkeep
```

### 修改二：`package.json` 新增测试脚本

在 `"scripts"` 部分新增两行（保持现有脚本不动）：

```json
{
  "scripts": {
    "doctor": "node ./bin/grok-acp.mjs doctor",
    "smoke": "...",
    "smoke:write": "...",
    "lint": "...",
    "test:monitor": "...",
    "test": "node --test",
    "test:watch": "node --test --watch"
  }
}
```

**同时在 `package.json` 根级别新增 `engines` 字段**（如果不存在）：

```json
{
  "name": "grokacp",
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": { ... }
}
```

### 修改三：`test/smoke.test.mjs` — 改造现有 smoke 测试

将 `npm run smoke` 的逻辑改造为测试文件（可直接采用）：

```js
import { describe, it } from "node:test";
import assert from "node:assert";
import { spawnSync } from "node:child_process";
import path from "node:path";
import { fileURLToPath } from "node:url";

const moduleDir = path.dirname(fileURLToPath(import.meta.url));
const binPath = path.join(moduleDir, "..", "bin", "grok-acp.mjs");

describe("smoke tests", () => {
  it("grok-acp run with inline prompt should succeed", () => {
    const result = spawnSync("node", [
      binPath,
      "run",
      "--prompt-text",
      "用一句中文回复：grokACP smoke ok。",
      "--cwd",
      "/home/desk/dev/repos/zq",
      "--out-dir",
      "/tmp/smoke-test-runs",
      "--timeout-ms",
      "60000",
      "--quiet"
    ], {
      encoding: "utf8",
      timeout: 70000
    });

    // 验证：退出码 0，stderr 包含 sessionId
    assert.strictEqual(result.status, 0, `Expected exit code 0, got ${result.status}\nStderr: ${result.stderr}\nStdout: ${result.stdout}`);
    assert.match(result.stderr, /grokACP sessionId=/, "Expected stderr to contain sessionId");
  });

  it("grok-acp doctor should succeed", () => {
    const result = spawnSync("node", [binPath, "doctor"], {
      encoding: "utf8",
      timeout: 10000
    });

    assert.strictEqual(result.status, 0, `doctor failed with status ${result.status}\nStderr: ${result.stderr}`);
    assert.match(result.stdout, /grok binary:/, "Expected stdout to contain grok binary info");
  });
});
```

**说明：**
- 第一个测试是 `npm run smoke` 的等价物，但用 `spawnSync` 调用 `bin/grok-acp.mjs`
- 第二个测试是 `npm run doctor` 的等价物
- 超时设置比原 smoke 略长（60s → 70s），避免偶发超时
- `--out-dir` 改为 `/tmp/smoke-test-runs`，避免污染仓库

### 修改四：`CLAUDE.md` 补充测试章节

在 `## Development` 章节下（`### Verify changes` 之后）新增一段：

```markdown
### Run tests

```bash
npm test          # 运行所有测试（包括 smoke）
npm run test:watch  # 监听模式，文件改动自动重跑
node --test test/smoke.test.mjs  # 只跑 smoke 测试
```

**测试框架：** Node.js 原生 `node:test`（需要 Node 20+），零 npm 依赖。

**测试文件命名：** `test/<name>.test.mjs`

**写测试时遵循 TDD 模式：**
1. 先写测试（包含所有边界情况）
2. 验证测试失败（红）
3. 写实现
4. 验证测试通过（绿）

详见 `工程师任务/2026-07-04-0340-AI友好化-测试与文档/PRD.md` §2.2。
```

### 修改五：`README.md` 补充测试章节（如果文件存在）

**先检查 `README.md` 是否存在**：

```bash
ls -la README.md
```

**如果存在**，在 "Development" 或 "Getting Started" 章节下新增：

```markdown
## Testing

Run all tests:

```bash
npm test
```

Run tests in watch mode:

```bash
npm test:watch
```

Tests are written using Node.js built-in `node:test` (requires Node 20+). No external dependencies needed.
```

**如果不存在**，跳过本步骤，在回执中说明"README.md 不存在，已跳过"。

---

## 验收（你必须自己执行并在回执中给出结果）

### 步骤 1：验证测试框架可用

```bash
cd /home/desk/dev/repos/grokACP
npm test 2>&1 | head -20
```

**预期输出：** 能看到 `smoke tests` 两个测试开始运行（可能失败，因为 Grok CLI 可能不在线或超时，这是正常的）。重点是**测试框架本身能运行**，不是测试必须全过。

### 步骤 2：验证 package.json 零依赖

```bash
cd /home/desk/dev/repos/grokACP
grep -E '"(dependencies|devDependencies)"' package.json || echo "无依赖字段（正常）"
```

**预期输出：** 要么没有这两个字段，要么字段值为空对象 `{}`。

### 步骤 3：验证 engines 字段

```bash
cd /home/desk/dev/repos/grokACP
grep -A 1 '"engines"' package.json
```

**预期输出：** 包含 `"node": ">=20.0.0"`。

### 步骤 4：验证现有 smoke 不回归

```bash
cd /home/desk/dev/repos/grokACP
npm run smoke 2>&1 | tail -5
```

**预期输出：** 退出码 0（没有报错），说明原 smoke 脚本仍然可用。

---

## 收工前自审（小改动，可跳过 Critic）

本任务是基础设施搭建，改动文件 ≤ 5 个，无共享入口，**可以跳过 Critic 自审**。

---

## 回复格式

修改的文件清单、每个文件的关键改动点、验收命令输出结果（4 个步骤）、是否跳过 Critic（说明"小改动，无 Critic 自审"）。
