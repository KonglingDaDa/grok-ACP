# T8 Minor 打磨 — ContextChip 与 ThroughputChart 优化

**任务：** T8  
**项目根目录：** `/home/desk/dev/repos/grokACP`  
**完成时间：** 2026-07-04（北京时间）

---

## 1. 改动文件

| 文件 | 改动摘要 |
|------|----------|
| `ui/src/components/ContextChip.tsx` | 新增 `getContextStyle()`，为 ok/watch/medium/high/critical 五级分配独立半透明背景 + 同色边框；保留原有 `上下文 {pct} · {used}/{window}` 文案与 title tooltip |
| `ui/src/components/ThroughputChart.tsx` | 引入 GSAP `motionSafe` + `autoAlpha` 驱动 tooltip 淡入/淡出；hover 时显示相对时间戳 + 绿色加粗 TPS；mini/full 模式均支持 hover；修复 `plotRef` 未就绪时 hover 无响应（mousemove 前同步 `drawRef.current()`） |

**截图（Playwright 替代 Chrome DevTools MCP — MCP 浏览器实例被占用）：**

- `/home/desk/dev/repos/grokACP/.codex-artifacts/t8-screenshots/01-demo-grid-context-chips.png` — 卡片网格 ok（绿）/ watch（琥珀）梯度
- `/home/desk/dev/repos/grokACP/.codex-artifacts/t8-screenshots/03-chart-tooltip-hover.png` — 详情面板图表 hover tooltip
- `/home/desk/dev/repos/grokACP/.codex-artifacts/t8-screenshots/04-context-gradient-all-levels.png` — 五级 ContextChip 全梯度对照

---

## 2. 已运行命令与完整输出

### `npm run lint`

```
> grok-acp@0.1.0 lint
> node --check ./bin/grok-acp.mjs && ... (全部通过，exit 0)
```

### `npm run test:all`

```
# tests 27 — pass 27, fail 0
# vitest: 9 passed (format.test.ts)
```

### `npm run ui:build`

```
✓ built in 709ms
dist/assets/index-CyfTbL9I.js  266.25 kB
```

---

## 3. 自检结论（逐条对当前任务）

| 检查项 | 结果 |
|--------|------|
| ContextChip ok → 绿色半透明底 + 绿边框 | ✅ |
| ContextChip watch → 浅黄底；medium → 深黄底 | ✅（04 截图五级对照） |
| ContextChip high → 浅红底；critical → 深红底 + crit 边框 | ✅ |
| ContextChip 保留 pct · used/window 文案与 title | ✅ |
| context 为 null 时不渲染（设计契约） | ✅ 未改 |
| ThroughputChart hover 显示时间 + TPS | ✅ Playwright 验证 `00:59 · 0.0 TPS`，opacity=1 |
| Tooltip GSAP 150ms 淡入 / 100ms 淡出 | ✅ `motionSafe` + `token-ease-out` |
| Tooltip `pointer-events-none`，不挡鼠标 | ✅ |
| prefers-reduced-motion 瞬显/瞬隐 | ✅ `motionSafe` reduced 分支 `gsap.set` |
| hover 不影响 canvas 绘制 | ✅ 仅读 plotRef + scheduleDraw |
| 样本为空不报错 | ✅ `onMouseMove` 仅在 `samples.length > 0` 时绑定 |

---

## 4. 剩余风险或未做事项

- **边界检测**：tooltip 在屏幕右/上边缘可能被裁剪（任务标注为可选，本次未实现）。
- **Chrome DevTools MCP**：本机 profile 被占用，视觉验收改用 Playwright headless 截图。
- **Demo 数据**：仅覆盖 ok/watch 两级真实卡片；medium/high/critical 用注入对照条验证样式。

---

## 5. 提示词反馈

无